<!DOCTYPE html>
<html>
  <head>
    <%- include('partials/head') %> 
  </head>
  <body>
    <%- include('partials/navbar') %> 
    <header class="py-10 max-w-3xl mx-auto px-8">
      <h1 class="text-zinc-950 text-left text-5xl pb-2"><%- product.name %></h1>
      <p class="text-xl">Category: <%= product.category_name %></p>
    </header>
    <main class="max-w-3xl mx-auto flex flex-col justify-between px-8 pb-10 gap-8">
      <p><%- product.description %></p>
      <div>
        <p class="text-4xl text-zinc-950">$<%= product.price %></p>
        <p class="text-zinc-700"><%= product.in_stock %> left in stock.</p>
      </div>
      <div class="flex flex-col gap-4 justify-center items-center md:flex-row">
        <a href="/product/<%- product.slug %>/edit"class="block px-6 py-2 bg-emerald-700 text-white text-xl font-semibold rounded-lg hover:scale-105">Edit Product</a>
        <a href="/product/<%- product.slug %>/delete"class="block px-0 py-2 text-red-700 text-xl font-semibold hover:scale-105">Delete Product</a>
      </div>
    </main>
  </body>
</html>
